import 'package:flutter/material.dart';

class AuthTopSpacer extends StatelessWidget {
  const AuthTopSpacer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Spacer();
  }
}
